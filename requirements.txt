# RobStride control GUI dependencies
PySide6>=6.6.0       # Qt GUI
pyqtgraph>=0.13.0    # real-time plots
pyserial>=3.5        # Serial (AT) transport
python-can>=4.0.0    # SocketCAN transport (optional; only for can0 path)

# dev / test
pytest>=7.0.0
